# SUSTech Store Backend

The backend codes of `Deal!`, which is the project of SUSTech CS309 OOAD class of category SUSTech Store.

The backend uses `Springboot`, and the authorization method uses `Shiro` + `JWT`.